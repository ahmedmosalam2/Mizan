[project]
name = "mizan-framework-template"
version = "0.1.0"
description = "Template for Mizan framework adapters"
requires-python = ">=3.11"

dependencies = [
    # Add your framework here, e.g.:
    # "crewai>=0.80",
    # "langgraph>=0.2",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
