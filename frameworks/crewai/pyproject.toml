[project]
name = "mizan-crewai"
version = "2.0.0"
description = "CrewAI adapter for the Mizan multi-agent benchmark"
requires-python = ">=3.11"

dependencies = [
    "crewai[tools]>=0.80",
    "litellm>=1.40",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
